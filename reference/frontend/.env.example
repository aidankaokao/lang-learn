# 前端 build / dev 環境變數範本（起手檔）。複製成 frontend/.env 使用。
# 說明見 frontend-backend-integration.md §6。

# 路由部署用：掛在網域根就留 /（預設，本機 dev 用這個）；
# 正式 build 通常不在這裡填，而是由 build.sh 依部署層 .env 的 APP_ROUTE 帶入
# （--build-arg VITE_BASE_PATH=/<APP_ROUTE>/）。見 frontend-backend-integration.md §6。
VITE_BASE_PATH=/
