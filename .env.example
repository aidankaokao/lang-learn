# 部署層環境變數（docker-compose 讀）。複製成專案根目錄的 .env（不進版控）。
# 這是內網 / 自架機器的 docker-compose 部署用；Cloud Run 不吃這個檔（見 DEPLOY-CLOUDRUN.md）。

# ── image / 網路命名（docker-compose.yaml 與 build.sh 吃同一組）──
IMAGE_PREFIX=yt-learn
IMAGE_TAG=0.1
NETWORK_NAME=yt-learn-network

# ── 路由名稱（內外網共用）──
# 前端掛在 /<APP_ROUTE>/：內網 http://<ip>:<FRONTEND_PORT>/yt-learn/
# 改動後前端 image 要重新 build（VITE_BASE_PATH 由 build.sh 帶入）
APP_ROUTE=yt-learn

# ── 對外 port ──
FRONTEND_PORT=8080
BACKEND_PORT=8000          # 僅開發期在 compose 打開 backend.ports 時用到

# ── labels（容器歸屬盤點）──
DEVELOPER_NAME=aidan
PROJECT_PATH=/home/aidankao/aidan/projects/yt-learn
